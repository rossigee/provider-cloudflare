apiVersion: dns.cloudflare.m.crossplane.io/v1beta1
kind: Record
metadata:
  namespace: default
  name: apex
spec:
  forProvider:
    zone: "your-zone-id"
    name: crossplane
    content: 192.168.0.1
    proxied: false

  providerConfigRef:
    name: example
