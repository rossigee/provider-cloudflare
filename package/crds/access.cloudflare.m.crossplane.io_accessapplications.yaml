---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: accessapplications.access.cloudflare.m.crossplane.io
spec:
  group: access.cloudflare.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - cloudflare
    kind: AccessApplication
    listKind: AccessApplicationList
    plural: accessapplications
    singular: accessapplication
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .spec.forProvider.domain
      name: DOMAIN
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: A AccessApplication represents a Cloudflare Access Application
          for Zero Trust security.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: A AccessApplicationSpec defines the desired state of an Access
              Application.
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                description: AccessApplicationParameters define the desired state
                  of a Cloudflare Access Application.
                properties:
                  accountId:
                    description: AccountID is the account ID where this access application
                      will be created.
                    type: string
                  allowedIdps:
                    description: AllowedIdps is a list of allowed Identity Provider
                      IDs.
                    items:
                      type: string
                    type: array
                  appLauncherVisible:
                    description: AppLauncherVisible indicates whether the app is visible
                      in the app launcher.
                    type: boolean
                  autoRedirectToIdentity:
                    description: AutoRedirectToIdentity indicates whether to automatically
                      redirect to the identity provider.
                    type: boolean
                  bgColor:
                    description: BgColor is the background color for the application.
                    type: string
                  domain:
                    description: Domain is the domain for the access application.
                    type: string
                  enableBindingCookie:
                    description: EnableBindingCookie indicates whether to enable binding
                      cookie.
                    type: boolean
                  footerLinks:
                    description: FooterLinks is a list of footer links for the application.
                    items:
                      description: AccessApplicationFooterLink defines a footer link
                        for the access application.
                      properties:
                        name:
                          description: Name is the name of the footer link.
                          type: string
                        url:
                          description: URL is the URL of the footer link.
                          type: string
                      required:
                      - name
                      - url
                      type: object
                    type: array
                  headerBgColor:
                    description: HeaderBgColor is the header background color for
                      the application.
                    type: string
                  landingPageDesign:
                    description: LandingPageDesign defines the landing page design.
                    properties:
                      buttonColor:
                        description: ButtonColor is the color of the button on the
                          landing page.
                        type: string
                      buttonTextColor:
                        description: ButtonTextColor is the text color of the button
                          on the landing page.
                        type: string
                      imageUrl:
                        description: ImageURL is the URL of the image for the landing
                          page.
                        type: string
                      message:
                        description: Message is the message of the landing page.
                        type: string
                      title:
                        description: Title is the title of the landing page.
                        type: string
                    type: object
                  logoUrl:
                    description: LogoURL is the URL of the logo for the application.
                    type: string
                  name:
                    description: Name is the name of the access application.
                    type: string
                  policies:
                    description: Policies references the policies to apply to this
                      application.
                    items:
                      type: string
                    type: array
                  serviceAuth401Redirect:
                    description: ServiceAuth401Redirect indicates whether to enable
                      401 redirect for service auth.
                    type: boolean
                  sessionDuration:
                    description: SessionDuration is the lifespan of the access application.
                    type: string
                  skipInterstitial:
                    description: SkipInterstitial indicates whether to skip the interstitial
                      page.
                    type: boolean
                  type:
                    description: Type is the type of the access application.
                    enum:
                    - self_hosted
                    - ssh
                    - browser_ssh
                    - warp
                    - bookmark
                    - saas
                    - app_launcher
                    - device_enrollment_permissions
                    - browser_isolation_permissions
                    type: string
                  zoneId:
                    description: ZoneID is the zone ID where this access application
                      will be created.
                    type: string
                required:
                - accountId
                - domain
                - name
                - type
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
          status:
            description: A AccessApplicationStatus represents the observed state of
              an Access Application.
            properties:
              atProvider:
                description: AccessApplicationObservation are the observable fields
                  of an Access Application.
                properties:
                  allowedIdps:
                    description: AllowedIdps is a list of allowed Identity Provider
                      IDs.
                    items:
                      type: string
                    type: array
                  appLauncherVisible:
                    description: AppLauncherVisible indicates whether the app is visible
                      in the app launcher.
                    type: boolean
                  aud:
                    description: Aud is the audience tag for the application.
                    type: string
                  autoRedirectToIdentity:
                    description: AutoRedirectToIdentity indicates whether to automatically
                      redirect to the identity provider.
                    type: boolean
                  bgColor:
                    description: BgColor is the background color for the application.
                    type: string
                  createdAt:
                    description: CreatedAt is the timestamp when the application was
                      created.
                    format: date-time
                    type: string
                  domain:
                    description: Domain is the domain for the access application.
                    type: string
                  enableBindingCookie:
                    description: EnableBindingCookie indicates whether to enable binding
                      cookie.
                    type: boolean
                  footerLinks:
                    description: FooterLinks is a list of footer links for the application.
                    items:
                      description: AccessApplicationFooterLink defines a footer link
                        for the access application.
                      properties:
                        name:
                          description: Name is the name of the footer link.
                          type: string
                        url:
                          description: URL is the URL of the footer link.
                          type: string
                      required:
                      - name
                      - url
                      type: object
                    type: array
                  headerBgColor:
                    description: HeaderBgColor is the header background color for
                      the application.
                    type: string
                  id:
                    description: ID is the unique identifier of the access application.
                    type: string
                  landingPageDesign:
                    description: LandingPageDesign defines the landing page design.
                    properties:
                      buttonColor:
                        description: ButtonColor is the color of the button on the
                          landing page.
                        type: string
                      buttonTextColor:
                        description: ButtonTextColor is the text color of the button
                          on the landing page.
                        type: string
                      imageUrl:
                        description: ImageURL is the URL of the image for the landing
                          page.
                        type: string
                      message:
                        description: Message is the message of the landing page.
                        type: string
                      title:
                        description: Title is the title of the landing page.
                        type: string
                    type: object
                  logoUrl:
                    description: LogoURL is the URL of the logo for the application.
                    type: string
                  name:
                    description: Name is the name of the access application.
                    type: string
                  serviceAuth401Redirect:
                    description: ServiceAuth401Redirect indicates whether to enable
                      401 redirect for service auth.
                    type: boolean
                  sessionDuration:
                    description: SessionDuration is the lifespan of the access application.
                    type: string
                  skipInterstitial:
                    description: SkipInterstitial indicates whether to skip the interstitial
                      page.
                    type: boolean
                  type:
                    description: Type is the type of the access application.
                    type: string
                  updatedAt:
                    description: UpdatedAt is the timestamp when the application was
                      last updated.
                    format: date-time
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastHandledReconcileAt:
                description: |-
                  LastHandledReconcileAt holds the value of the most recent
                  reconcile-requested-at annotation token that the controller has
                  processed. Users can compare this to the annotation to determine
                  whether a reconcile request has been handled.
                type: string
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
