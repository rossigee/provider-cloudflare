---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.0
  name: cacherules.cache.cloudflare.m.crossplane.io
spec:
  group: cache.cloudflare.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - cloudflare
    kind: CacheRule
    listKind: CacheRuleList
    plural: cacherules
    singular: cacherule
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: A CacheRule represents a Cloudflare Cache Rule for advanced caching
          control.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: A CacheRuleSpec defines the desired state of a Cache Rule.
            properties:
              forProvider:
                description: CacheRuleParameters define the desired state of a Cloudflare
                  Cache Rule
                properties:
                  action:
                    default: set_cache_settings
                    description: Action is the cache action to take.
                    enum:
                    - set_cache_settings
                    - bypass_cache
                    type: string
                  actionParameters:
                    description: ActionParameters specifies the action parameters
                      for the cache rule.
                    properties:
                      browserTtl:
                        description: BrowserTTL defines the browser cache TTL settings.
                        properties:
                          default:
                            description: Default is the default browser TTL in seconds.
                            format: int64
                            type: integer
                          mode:
                            description: Mode defines the browser TTL mode.
                            enum:
                            - override_origin
                            - respect_origin
                            - bypass_by_default
                            type: string
                        type: object
                      cache:
                        description: Cache indicates whether to cache or not cache.
                        type: boolean
                      cacheKey:
                        description: CacheKey defines how the cache key is constructed.
                        properties:
                          cacheDeceptionArmor:
                            description: CacheDeceptionArmor indicates whether to
                              enable cache deception armor.
                            type: boolean
                          customKey:
                            description: CustomKey defines custom cache key settings.
                            properties:
                              cookie:
                                description: Cookie defines custom cookie settings.
                                properties:
                                  checkPresence:
                                    description: CheckPresence is a list of cookies
                                      to check for presence.
                                    items:
                                      type: string
                                    type: array
                                  include:
                                    description: Include is a list of cookies to include.
                                    items:
                                      type: string
                                    type: array
                                type: object
                              header:
                                description: Header defines custom header settings.
                                properties:
                                  checkPresence:
                                    description: CheckPresence is a list of headers
                                      to check for presence.
                                    items:
                                      type: string
                                    type: array
                                  exclude:
                                    description: Exclude is a list of headers to exclude.
                                    items:
                                      type: string
                                    type: array
                                  include:
                                    description: Include is a list of headers to include.
                                    items:
                                      type: string
                                    type: array
                                type: object
                              host:
                                description: Host defines custom host settings.
                                properties:
                                  resolved:
                                    description: Resolved indicates whether to use
                                      resolved host.
                                    type: boolean
                                type: object
                              query:
                                description: Query defines custom query string settings.
                                properties:
                                  exclude:
                                    description: Exclude is a list of query string
                                      parameters to exclude.
                                    items:
                                      type: string
                                    type: array
                                  include:
                                    description: Include is a list of query string
                                      parameters to include.
                                    items:
                                      type: string
                                    type: array
                                type: object
                              user:
                                description: User defines custom user settings.
                                properties:
                                  deviceType:
                                    description: DeviceType indicates whether to vary
                                      by device type.
                                    type: boolean
                                  geo:
                                    description: Geo indicates whether to vary by
                                      geo.
                                    type: boolean
                                  lang:
                                    description: Lang indicates whether to vary by
                                      language.
                                    type: boolean
                                type: object
                            type: object
                          ignoreQueryStringsOrder:
                            description: IgnoreQueryStringsOrder indicates whether
                              to ignore query string order.
                            type: boolean
                        type: object
                      edgeTtl:
                        description: EdgeTTL defines the edge cache TTL settings.
                        properties:
                          default:
                            description: Default is the default edge TTL in seconds.
                            format: int64
                            type: integer
                          mode:
                            description: Mode defines the edge TTL mode.
                            enum:
                            - override_origin
                            - respect_origin
                            - bypass_by_default
                            type: string
                          statusCodeTtl:
                            description: StatusCodeTTL is a list of status code specific
                              TTL settings.
                            items:
                              description: StatusCodeTTL defines TTL settings for
                                specific status codes
                              properties:
                                statusCode:
                                  description: StatusCode is the specific status code.
                                  format: int64
                                  type: integer
                                statusCodeRange:
                                  description: StatusCodeRange defines a range of
                                    status codes.
                                  properties:
                                    from:
                                      description: From is the start of the status
                                        code range.
                                      format: int64
                                      type: integer
                                    to:
                                      description: To is the end of the status code
                                        range.
                                      format: int64
                                      type: integer
                                  required:
                                  - from
                                  - to
                                  type: object
                                value:
                                  description: Value is the TTL value in seconds.
                                  format: int64
                                  type: integer
                              required:
                              - value
                              type: object
                            type: array
                        type: object
                      originErrorPagePassThru:
                        description: OriginErrorPagePassThru indicates whether to
                          pass through origin error pages.
                        type: boolean
                      respectOrigin:
                        description: RespectOrigin indicates whether to respect origin
                          cache headers.
                        type: boolean
                      serveStale:
                        description: ServeStale defines the serve stale settings.
                        properties:
                          disableStaleWhileUpdating:
                            description: DisableStaleWhileUpdating indicates whether
                              to disable serve stale while updating.
                            type: boolean
                        type: object
                    type: object
                  description:
                    description: Description is a description of the cache rule.
                    type: string
                  enabled:
                    description: Enabled specifies whether the cache rule is enabled.
                    type: boolean
                  expression:
                    description: |-
                      Expression is the Cloudflare expression that determines when this cache rule applies.
                      Examples:
                      - "(http.request.uri.path contains \"/images/\")"
                      - "(http.request.uri.path.extension eq \"jpg\")"
                      - "(http.host eq \"example.com\" and http.request.uri.path.extension in {\"css\" \"js\"})"
                    type: string
                  name:
                    description: Name is the name of the cache rule.
                    type: string
                  zone:
                    description: |-
                      Zone is the zone ID where this cache rule will be applied.
                      Cache rules are zone-scoped resources.
                    type: string
                required:
                - expression
                - name
                - zone
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
          status:
            description: A CacheRuleStatus represents the observed state of a Cache
              Rule.
            properties:
              atProvider:
                description: CacheRuleObservation is the observable fields of a Cache
                  Rule.
                properties:
                  id:
                    description: ID of the created cache rule.
                    type: string
                  lastModified:
                    description: LastModified indicates when the cache rule was last
                      modified.
                    type: string
                  phase:
                    description: Phase indicates the phase where the cache rule is
                      executed.
                    type: string
                  rulesetId:
                    description: RulesetID of the ruleset containing the cache rule.
                    type: string
                  version:
                    description: Version indicates the version of the cache rule.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastHandledReconcileAt:
                description: |-
                  LastHandledReconcileAt holds the value of the most recent
                  reconcile-requested-at annotation token that the controller has
                  processed. Users can compare this to the annotation to determine
                  whether a reconcile request has been handled.
                type: string
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
