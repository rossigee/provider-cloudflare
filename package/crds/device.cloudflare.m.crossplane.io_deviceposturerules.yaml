---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: deviceposturerules.device.cloudflare.m.crossplane.io
spec:
  group: device.cloudflare.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - cloudflare
    kind: DevicePostureRule
    listKind: DevicePostureRuleList
    plural: deviceposturerules
    singular: deviceposturerule
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .spec.forProvider.name
      name: NAME
      type: string
    - jsonPath: .spec.forProvider.type
      name: TYPE
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: A DevicePostureRule represents a Cloudflare Device Posture Rule
          for endpoint security.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: A DevicePostureRuleSpec defines the desired state of a Device
              Posture Rule.
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                description: DevicePostureRuleParameters define the desired state
                  of a Cloudflare Device Posture Rule.
                properties:
                  accountId:
                    description: AccountID is the account ID where this device posture
                      rule will be created.
                    type: string
                  description:
                    description: Description is the description of the device posture
                      rule.
                    type: string
                  expiration:
                    description: Expiration is the expiration configuration for the
                      device posture rule.
                    type: string
                  input:
                    description: Input is the input configuration for the device posture
                      rule.
                    properties:
                      certificateId:
                        description: CertificateID is the certificate ID.
                        type: string
                      checkDisks:
                        description: CheckDisks is a list of disks to check.
                        items:
                          type: string
                        type: array
                      checkPrivateKey:
                        description: CheckPrivateKey indicates whether to check for
                          private key.
                        type: boolean
                      cn:
                        description: CN is the common name.
                        type: string
                      complianceStatus:
                        description: ComplianceStatus is the compliance status.
                        type: string
                      connectionId:
                        description: ConnectionID is the connection ID.
                        type: string
                      domain:
                        description: Domain is the domain for domain joined rules.
                        type: string
                      enabled:
                        description: Enabled indicates whether the rule is enabled.
                        type: boolean
                      exists:
                        description: Exists indicates whether the file should exist.
                        type: boolean
                      id:
                        description: ID is the ID for the input.
                        type: string
                      operator:
                        description: Operator is the comparison operator.
                        enum:
                        - '>'
                        - <
                        - '>='
                        - <=
                        - ==
                        type: string
                      os:
                        description: OS is the operating system version.
                        type: string
                      overall:
                        description: Overall indicates the overall posture result.
                        type: string
                      path:
                        description: Path is the path for file-based rules.
                        type: string
                      requireAll:
                        description: RequireAll indicates whether all conditions must
                          be met.
                        type: boolean
                      running:
                        description: Running indicates whether the application should
                          be running.
                        type: boolean
                      sensorConfig:
                        description: SensorConfig is the sensor configuration.
                        type: string
                      sha256:
                        description: SHA256 is the SHA256 hash.
                        type: string
                      state:
                        description: State is the state requirement.
                        type: string
                      thumbprint:
                        description: Thumbprint is the certificate thumbprint.
                        type: string
                      version:
                        description: Version is the version requirement.
                        type: string
                    type: object
                  match:
                    description: Match is the match criteria for the device posture
                      rule.
                    items:
                      description: DevicePostureRuleMatch defines match criteria for
                        device posture rules.
                      properties:
                        platform:
                          description: Platform is the platform to match.
                          enum:
                          - windows
                          - mac
                          - linux
                          - android
                          - ios
                          type: string
                      type: object
                    type: array
                  name:
                    description: Name is the name of the device posture rule.
                    type: string
                  schedule:
                    description: Schedule is the schedule for the device posture rule.
                    type: string
                  type:
                    description: Type is the type of device posture rule.
                    enum:
                    - file
                    - application
                    - tanium
                    - gateway
                    - warp
                    - disk_encryption
                    - serial_number
                    - decryption_key
                    - os_version
                    - domain_joined
                    - os
                    - dFirewall
                    - client_certificate
                    - client_certificate_v2
                    - workspace_one
                    - sentinelone
                    - intune
                    - crowdstrike
                    - firewall
                    - disk_encryption_browsers
                    - unique_client_id
                    - kolide
                    type: string
                required:
                - accountId
                - name
                - type
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
          status:
            description: A DevicePostureRuleStatus represents the observed state of
              a Device Posture Rule.
            properties:
              atProvider:
                description: DevicePostureRuleObservation are the observable fields
                  of a Device Posture Rule.
                properties:
                  createdAt:
                    description: CreatedAt is the timestamp when the rule was created.
                    format: date-time
                    type: string
                  description:
                    description: Description is the description of the device posture
                      rule.
                    type: string
                  expiration:
                    description: Expiration is the expiration configuration for the
                      device posture rule.
                    type: string
                  id:
                    description: ID is the unique identifier of the device posture
                      rule.
                    type: string
                  input:
                    description: Input is the input configuration for the device posture
                      rule.
                    properties:
                      certificateId:
                        description: CertificateID is the certificate ID.
                        type: string
                      checkDisks:
                        description: CheckDisks is a list of disks to check.
                        items:
                          type: string
                        type: array
                      checkPrivateKey:
                        description: CheckPrivateKey indicates whether to check for
                          private key.
                        type: boolean
                      cn:
                        description: CN is the common name.
                        type: string
                      complianceStatus:
                        description: ComplianceStatus is the compliance status.
                        type: string
                      connectionId:
                        description: ConnectionID is the connection ID.
                        type: string
                      domain:
                        description: Domain is the domain for domain joined rules.
                        type: string
                      enabled:
                        description: Enabled indicates whether the rule is enabled.
                        type: boolean
                      exists:
                        description: Exists indicates whether the file should exist.
                        type: boolean
                      id:
                        description: ID is the ID for the input.
                        type: string
                      operator:
                        description: Operator is the comparison operator.
                        enum:
                        - '>'
                        - <
                        - '>='
                        - <=
                        - ==
                        type: string
                      os:
                        description: OS is the operating system version.
                        type: string
                      overall:
                        description: Overall indicates the overall posture result.
                        type: string
                      path:
                        description: Path is the path for file-based rules.
                        type: string
                      requireAll:
                        description: RequireAll indicates whether all conditions must
                          be met.
                        type: boolean
                      running:
                        description: Running indicates whether the application should
                          be running.
                        type: boolean
                      sensorConfig:
                        description: SensorConfig is the sensor configuration.
                        type: string
                      sha256:
                        description: SHA256 is the SHA256 hash.
                        type: string
                      state:
                        description: State is the state requirement.
                        type: string
                      thumbprint:
                        description: Thumbprint is the certificate thumbprint.
                        type: string
                      version:
                        description: Version is the version requirement.
                        type: string
                    type: object
                  match:
                    description: Match is the match criteria for the device posture
                      rule.
                    items:
                      description: DevicePostureRuleMatch defines match criteria for
                        device posture rules.
                      properties:
                        platform:
                          description: Platform is the platform to match.
                          enum:
                          - windows
                          - mac
                          - linux
                          - android
                          - ios
                          type: string
                      type: object
                    type: array
                  name:
                    description: Name is the name of the device posture rule.
                    type: string
                  schedule:
                    description: Schedule is the schedule for the device posture rule.
                    type: string
                  type:
                    description: Type is the type of device posture rule.
                    type: string
                  updatedAt:
                    description: UpdatedAt is the timestamp when the rule was last
                      updated.
                    format: date-time
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastHandledReconcileAt:
                description: |-
                  LastHandledReconcileAt holds the value of the most recent
                  reconcile-requested-at annotation token that the controller has
                  processed. Users can compare this to the annotation to determine
                  whether a reconcile request has been handled.
                type: string
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
