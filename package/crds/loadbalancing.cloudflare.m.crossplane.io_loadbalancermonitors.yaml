---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.0
  name: loadbalancermonitors.loadbalancing.cloudflare.m.crossplane.io
spec:
  group: loadbalancing.cloudflare.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - cloudflare
    kind: LoadBalancerMonitor
    listKind: LoadBalancerMonitorList
    plural: loadbalancermonitors
    singular: loadbalancermonitor
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.atProvider.id
      name: ID
      type: string
    - jsonPath: .spec.forProvider.type
      name: TYPE
      type: string
    - jsonPath: .spec.forProvider.interval
      name: INTERVAL
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: LoadBalancerMonitor is a managed resource that represents a Cloudflare
          Load Balancer Monitor
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: LoadBalancerMonitorSpec defines the desired state of LoadBalancerMonitor
            properties:
              forProvider:
                description: LoadBalancerMonitorParameters define the desired state
                  of a Cloudflare Load Balancer Monitor
                properties:
                  account:
                    description: |-
                      Account is the account ID where this monitor will be created.
                      Either Account or Zone must be specified, but not both.
                    type: string
                  allowInsecure:
                    description: |-
                      AllowInsecure indicates whether to allow insecure connections.
                      Only applies to HTTPS monitors.
                    type: boolean
                  consecutiveDown:
                    description: |-
                      ConsecutiveDown is the number of consecutive failed health checks required
                      before marking an origin as unhealthy.
                      Valid range: 1-20.
                    type: integer
                  consecutiveUp:
                    description: |-
                      ConsecutiveUp is the number of consecutive successful health checks required
                      before marking an origin as healthy.
                      Valid range: 1-20.
                    type: integer
                  description:
                    description: Description is a human-readable description of the
                      monitor.
                    type: string
                  expectedBody:
                    description: |-
                      ExpectedBody is the expected response body for the health check.
                      Only applies to HTTP/HTTPS monitors.
                    type: string
                  expectedCodes:
                    description: |-
                      ExpectedCodes is the expected HTTP response code or code range.
                      Examples: "200", "2xx", "200,202,301"
                      Only applies to HTTP/HTTPS monitors.
                    type: string
                  followRedirects:
                    description: |-
                      FollowRedirects indicates whether to follow redirects.
                      Only applies to HTTP/HTTPS monitors.
                    type: boolean
                  header:
                    additionalProperties:
                      items:
                        type: string
                      type: array
                    description: Header contains HTTP headers to send with the health
                      check request.
                    type: object
                  interval:
                    description: |-
                      Interval is the interval in seconds between health checks.
                      Valid range: 5-3600 seconds.
                    type: integer
                  method:
                    description: |-
                      Method is the HTTP method to use for the health check (only for http/https).
                      Valid values: "GET", "HEAD"
                    type: string
                  path:
                    description: Path is the path to use for the health check (only
                      for http/https).
                    type: string
                  port:
                    description: |-
                      Port is the port to use for the health check.
                      If not specified, defaults to 80 for HTTP and 443 for HTTPS.
                    type: integer
                  probeZone:
                    description: |-
                      ProbeZone is the zone ID to use for the probe.
                      If not specified, probes will be sent from all Cloudflare data centers.
                    type: string
                  retries:
                    description: |-
                      Retries is the number of retries to attempt in case of a timeout before marking as failed.
                      Valid range: 0-5 retries.
                    type: integer
                  timeout:
                    description: |-
                      Timeout is the timeout in seconds before marking the health check as failed.
                      Valid range: 1-10 seconds.
                    type: integer
                  type:
                    description: |-
                      Type is the protocol to use for the health check.
                      Valid values: "http", "https", "tcp", "udp_icmp", "icmp_ping", "smtp"
                    type: string
                  zone:
                    description: |-
                      Zone is the zone ID where this monitor will be created.
                      Either Account or Zone must be specified, but not both.
                    type: string
                required:
                - type
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
          status:
            description: LoadBalancerMonitorStatus defines the observed state of LoadBalancerMonitor
            properties:
              atProvider:
                description: LoadBalancerMonitorObservation represents the observed
                  state of a Cloudflare Load Balancer Monitor
                properties:
                  createdOn:
                    description: CreatedOn is when the monitor was created.
                    type: string
                  id:
                    description: ID is the monitor ID.
                    type: string
                  modifiedOn:
                    description: ModifiedOn is when the monitor was last modified.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastHandledReconcileAt:
                description: |-
                  LastHandledReconcileAt holds the value of the most recent
                  reconcile-requested-at annotation token that the controller has
                  processed. Users can compare this to the annotation to determine
                  whether a reconcile request has been handled.
                type: string
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
