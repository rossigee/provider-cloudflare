---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: ratelimits.security.cloudflare.m.crossplane.io
spec:
  group: security.cloudflare.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - cloudflare
    kind: RateLimit
    listKind: RateLimitList
    plural: ratelimits
    singular: ratelimit
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: A RateLimit represents a Cloudflare Rate Limit rule for traffic
          control.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: A RateLimitSpec defines the desired state of a Rate Limit.
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                description: RateLimitParameters define the desired state of a Cloudflare
                  Rate Limit rule.
                properties:
                  action:
                    description: Action defines what action to take when the rate
                      limit is exceeded.
                    properties:
                      mode:
                        description: Mode defines the action mode.
                        enum:
                        - simulate
                        - ban
                        - challenge
                        - js_challenge
                        - managed_challenge
                        type: string
                      response:
                        description: Response defines the response to send for challenge
                          actions.
                        properties:
                          body:
                            description: Body is the response body content.
                            type: string
                          contentType:
                            description: ContentType is the content type of the response.
                            type: string
                        type: object
                      timeout:
                        description: Timeout is the timeout duration in seconds for
                          ban actions.
                        maximum: 86400
                        minimum: 10
                        type: integer
                    required:
                    - mode
                    type: object
                  bypass:
                    description: Bypass is a list of characteristics that will bypass
                      this rate limit.
                    items:
                      description: RateLimitKeyValue represents a key-value pair for
                        matching.
                      properties:
                        name:
                          description: Name is the name of the key.
                          type: string
                        value:
                          description: Value is the value to match.
                          type: string
                      required:
                      - name
                      - value
                      type: object
                    type: array
                  correlate:
                    description: Correlate defines how requests are correlated for
                      rate limiting.
                    properties:
                      by:
                        description: By is the correlation method.
                        enum:
                        - nat
                        type: string
                    type: object
                  description:
                    description: Description is a human-readable description of the
                      rate limit.
                    type: string
                  disabled:
                    description: Disabled indicates if the rate limit is disabled.
                    type: boolean
                  match:
                    description: Match defines the traffic matching rules for this
                      rate limit.
                    properties:
                      request:
                        description: Request defines request matching criteria.
                        properties:
                          methods:
                            description: Methods is a list of HTTP methods to match.
                            items:
                              type: string
                            type: array
                          schemes:
                            description: Schemes is a list of URI schemes to match.
                            items:
                              type: string
                            type: array
                          url:
                            description: URL is the URL pattern to match.
                            type: string
                        type: object
                      response:
                        description: Response defines response matching criteria.
                        properties:
                          headers:
                            description: Headers defines header matching criteria.
                            items:
                              description: RateLimitKeyValue represents a key-value
                                pair for matching.
                              properties:
                                name:
                                  description: Name is the name of the key.
                                  type: string
                                value:
                                  description: Value is the value to match.
                                  type: string
                              required:
                              - name
                              - value
                              type: object
                            type: array
                          originTraffic:
                            description: OriginTraffic indicates whether to count
                              origin traffic.
                            type: boolean
                          statuses:
                            description: Statuses is a list of HTTP status codes to
                              match.
                            items:
                              type: integer
                            type: array
                        type: object
                    type: object
                  period:
                    description: Period is the time period in seconds during which
                      the threshold applies.
                    maximum: 86400
                    minimum: 1
                    type: integer
                  threshold:
                    description: Threshold is the number of requests allowed within
                      the specified period.
                    minimum: 1
                    type: integer
                  zone:
                    description: Zone is the zone ID where this rate limit will be
                      applied.
                    type: string
                required:
                - action
                - match
                - period
                - threshold
                - zone
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
          status:
            description: A RateLimitStatus represents the observed state of a Rate
              Limit.
            properties:
              atProvider:
                description: RateLimitObservation are the observable fields of a Rate
                  Limit.
                properties:
                  action:
                    description: Action defines what action to take when the rate
                      limit is exceeded.
                    properties:
                      mode:
                        description: Mode defines the action mode.
                        enum:
                        - simulate
                        - ban
                        - challenge
                        - js_challenge
                        - managed_challenge
                        type: string
                      response:
                        description: Response defines the response to send for challenge
                          actions.
                        properties:
                          body:
                            description: Body is the response body content.
                            type: string
                          contentType:
                            description: ContentType is the content type of the response.
                            type: string
                        type: object
                      timeout:
                        description: Timeout is the timeout duration in seconds for
                          ban actions.
                        maximum: 86400
                        minimum: 10
                        type: integer
                    required:
                    - mode
                    type: object
                  bypass:
                    description: Bypass is a list of characteristics that will bypass
                      this rate limit.
                    items:
                      description: RateLimitKeyValue represents a key-value pair for
                        matching.
                      properties:
                        name:
                          description: Name is the name of the key.
                          type: string
                        value:
                          description: Value is the value to match.
                          type: string
                      required:
                      - name
                      - value
                      type: object
                    type: array
                  correlate:
                    description: Correlate defines how requests are correlated for
                      rate limiting.
                    properties:
                      by:
                        description: By is the correlation method.
                        enum:
                        - nat
                        type: string
                    type: object
                  description:
                    description: Description is a human-readable description of the
                      rate limit.
                    type: string
                  disabled:
                    description: Disabled indicates if the rate limit is disabled.
                    type: boolean
                  id:
                    description: ID is the unique identifier of the rate limit.
                    type: string
                  match:
                    description: Match defines the traffic matching rules for this
                      rate limit.
                    properties:
                      request:
                        description: Request defines request matching criteria.
                        properties:
                          methods:
                            description: Methods is a list of HTTP methods to match.
                            items:
                              type: string
                            type: array
                          schemes:
                            description: Schemes is a list of URI schemes to match.
                            items:
                              type: string
                            type: array
                          url:
                            description: URL is the URL pattern to match.
                            type: string
                        type: object
                      response:
                        description: Response defines response matching criteria.
                        properties:
                          headers:
                            description: Headers defines header matching criteria.
                            items:
                              description: RateLimitKeyValue represents a key-value
                                pair for matching.
                              properties:
                                name:
                                  description: Name is the name of the key.
                                  type: string
                                value:
                                  description: Value is the value to match.
                                  type: string
                              required:
                              - name
                              - value
                              type: object
                            type: array
                          originTraffic:
                            description: OriginTraffic indicates whether to count
                              origin traffic.
                            type: boolean
                          statuses:
                            description: Statuses is a list of HTTP status codes to
                              match.
                            items:
                              type: integer
                            type: array
                        type: object
                    type: object
                  period:
                    description: Period is the time period in seconds during which
                      the threshold applies.
                    type: integer
                  threshold:
                    description: Threshold is the number of requests allowed within
                      the specified period.
                    type: integer
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastHandledReconcileAt:
                description: |-
                  LastHandledReconcileAt holds the value of the most recent
                  reconcile-requested-at annotation token that the controller has
                  processed. Users can compare this to the annotation to determine
                  whether a reconcile request has been handled.
                type: string
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
