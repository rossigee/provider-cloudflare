---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: tunnels.tunnel.cloudflare.m.crossplane.io
spec:
  group: tunnel.cloudflare.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - cloudflare
    kind: Tunnel
    listKind: TunnelList
    plural: tunnels
    singular: tunnel
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .spec.forProvider.name
      name: NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: A Tunnel represents a Cloudflare Tunnel for secure connectivity.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: A TunnelSpec defines the desired state of a Tunnel.
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                description: TunnelParameters define the desired state of a Cloudflare
                  Tunnel.
                properties:
                  accountId:
                    description: AccountID is the account ID where this tunnel will
                      be created.
                    type: string
                  configSrc:
                    description: ConfigSrc is the configuration source for the tunnel.
                    properties:
                      cloudflareAccess:
                        description: CloudflareAccess is the Cloudflare Access configuration.
                        properties:
                          audTag:
                            description: AudTag is a list of audience tags.
                            items:
                              type: string
                            type: array
                          defaultPassesThrough:
                            description: DefaultPassesThrough indicates if requests
                              pass through by default.
                            type: boolean
                          teamName:
                            description: TeamName is the team name for Cloudflare
                              Access.
                            type: string
                        type: object
                      ingress:
                        description: Ingress is a list of ingress rules.
                        items:
                          description: TunnelIngress defines an ingress rule.
                          properties:
                            hostname:
                              description: Hostname is the hostname for the ingress
                                rule.
                              type: string
                            originRequest:
                              description: OriginRequest is the origin request configuration
                                for this ingress rule.
                              properties:
                                bastionMode:
                                  description: BastionMode indicates whether to enable
                                    bastion mode.
                                  type: boolean
                                caFile:
                                  description: CAFile is the CA certificate file.
                                  items:
                                    type: string
                                  type: array
                                connectTimeout:
                                  description: ConnectTimeout is the connection timeout.
                                  type: integer
                                disableChunkedEncoding:
                                  description: DisableChunkedEncoding indicates whether
                                    to disable chunked encoding.
                                  type: boolean
                                http2Origin:
                                  description: HTTP2Origin indicates whether to use
                                    HTTP/2 for origin.
                                  type: boolean
                                httpHostHeader:
                                  description: HTTPHostHeader is the HTTP host header.
                                  type: string
                                keepAliveConnections:
                                  description: KeepAliveConnections is the number
                                    of keep alive connections.
                                  type: integer
                                keepAliveTimeout:
                                  description: KeepAliveTimeout is the keep alive
                                    timeout.
                                  type: integer
                                noHappyEyeballs:
                                  description: NoHappyEyeballs indicates whether to
                                    disable happy eyeballs.
                                  type: boolean
                                noTlsVerify:
                                  description: NoTLSVerify indicates whether to skip
                                    TLS verification.
                                  type: boolean
                                originServerName:
                                  description: OriginServerName is the origin server
                                    name.
                                  type: string
                                proxyAddress:
                                  description: ProxyAddress is the proxy address.
                                  type: string
                                proxyPort:
                                  description: ProxyPort is the proxy port.
                                  type: integer
                                proxyType:
                                  description: ProxyType is the proxy type.
                                  type: string
                                tcpKeepAlive:
                                  description: TCPKeepAlive is the TCP keep alive
                                    duration.
                                  type: integer
                                tlsTimeout:
                                  description: TLSTimeout is the TLS timeout.
                                  type: integer
                              type: object
                            path:
                              description: Path is the path for the ingress rule.
                              type: string
                            service:
                              description: Service is the service for the ingress
                                rule.
                              type: string
                          required:
                          - service
                          type: object
                        type: array
                      originRequest:
                        description: OriginRequest is the origin request configuration.
                        properties:
                          bastionMode:
                            description: BastionMode indicates whether to enable bastion
                              mode.
                            type: boolean
                          caFile:
                            description: CAFile is the CA certificate file.
                            items:
                              type: string
                            type: array
                          connectTimeout:
                            description: ConnectTimeout is the connection timeout.
                            type: integer
                          disableChunkedEncoding:
                            description: DisableChunkedEncoding indicates whether
                              to disable chunked encoding.
                            type: boolean
                          http2Origin:
                            description: HTTP2Origin indicates whether to use HTTP/2
                              for origin.
                            type: boolean
                          httpHostHeader:
                            description: HTTPHostHeader is the HTTP host header.
                            type: string
                          keepAliveConnections:
                            description: KeepAliveConnections is the number of keep
                              alive connections.
                            type: integer
                          keepAliveTimeout:
                            description: KeepAliveTimeout is the keep alive timeout.
                            type: integer
                          noHappyEyeballs:
                            description: NoHappyEyeballs indicates whether to disable
                              happy eyeballs.
                            type: boolean
                          noTlsVerify:
                            description: NoTLSVerify indicates whether to skip TLS
                              verification.
                            type: boolean
                          originServerName:
                            description: OriginServerName is the origin server name.
                            type: string
                          proxyAddress:
                            description: ProxyAddress is the proxy address.
                            type: string
                          proxyPort:
                            description: ProxyPort is the proxy port.
                            type: integer
                          proxyType:
                            description: ProxyType is the proxy type.
                            type: string
                          tcpKeepAlive:
                            description: TCPKeepAlive is the TCP keep alive duration.
                            type: integer
                          tlsTimeout:
                            description: TLSTimeout is the TLS timeout.
                            type: integer
                        type: object
                      warpRouting:
                        description: WarpRouting is the WARP routing configuration.
                        properties:
                          enabled:
                            description: Enabled indicates whether WARP routing is
                              enabled.
                            type: boolean
                        type: object
                    type: object
                  name:
                    description: Name is the name of the tunnel.
                    type: string
                  secret:
                    description: Secret is the secret key for the tunnel.
                    type: string
                required:
                - accountId
                - name
                - secret
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
          status:
            description: A TunnelStatus represents the observed state of a Tunnel.
            properties:
              atProvider:
                description: TunnelObservation are the observable fields of a Tunnel.
                properties:
                  connections:
                    description: Connections is a list of tunnel connections.
                    items:
                      description: TunnelConnection represents a tunnel connection.
                      properties:
                        clientId:
                          description: ClientID is the client ID.
                          type: string
                        clientVersion:
                          description: ClientVersion is the client version.
                          type: string
                        coloName:
                          description: ColoName is the colocation name.
                          type: string
                        id:
                          description: ID is the connection ID.
                          type: string
                        openedAt:
                          description: OpenedAt is when the connection was opened.
                          format: date-time
                          type: string
                        originIp:
                          description: OriginIP is the origin IP address.
                          type: string
                      type: object
                    type: array
                  createdAt:
                    description: CreatedAt is the timestamp when the tunnel was created.
                    format: date-time
                    type: string
                  deletedAt:
                    description: DeletedAt is the timestamp when the tunnel was deleted.
                    format: date-time
                    type: string
                  id:
                    description: ID is the unique identifier of the tunnel.
                    type: string
                  name:
                    description: Name is the name of the tunnel.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastHandledReconcileAt:
                description: |-
                  LastHandledReconcileAt holds the value of the most recent
                  reconcile-requested-at annotation token that the controller has
                  processed. Users can compare this to the annotation to determine
                  whether a reconcile request has been handled.
                type: string
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
